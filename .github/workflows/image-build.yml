name: build sample dotnet image
on:
  push:
    branches:
      - 'master'
jobs:
  build:
     runs-on: ubuntu-latest
     steps:
       - name: 'Checkout'
         uses: actions/checkout@v2
       - name: 'cloning repo'
         uses: actions/checkout@v2
       - name: 'Creating sample dotnet image'
         run: |
           docker ps
           docker build -t ghcr.io/zs-amrutha/dotnet -f Dockerfile .
       - name: 'docker login'
         run: |
           docker login -u zs-amrutha -p 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 ghcr.io
       - name: 'docker push'
         run: |
           docker push ghcr.io/zs-amrutha/dotnet     
           
             
    
  
